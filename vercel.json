{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install && pnpm --filter web exec prisma generate",
  "buildCommand": "pnpm --filter web build",
  "outputDirectory": "apps/web/.next"
}
